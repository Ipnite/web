<style is:global>
  .rafa-profile-page.ipnite-profile {
    max-width: 1280px;
    padding: 24px 24px 84px;
    color: #505050;
    font-family: "Roboto", sans-serif;
  }

  .rafa-profile-page.ipnite-profile h1,
  .rafa-profile-page.ipnite-profile h2,
  .rafa-profile-page.ipnite-profile h3,
  .rafa-profile-page.ipnite-profile .headline {
    font-family: "Roboto Slab", serif;
    font-style: normal;
    letter-spacing: -0.025em;
  }

  .rafa-profile-page.ipnite-profile .profile-hero {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    margin: 0 auto;
    padding: clamp(42px, 5vw, 70px);
    border: 1px solid rgba(107, 42, 47, 0.08);
    border-radius: clamp(28px, 3vw, 50px);
    background:
      radial-gradient(circle at 82% 24%, rgba(107, 42, 47, 0.13), transparent 34%),
      rgba(217, 217, 217, 0.16);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.52),
      inset 0 -1px 1px rgba(0, 0, 0, 0.08);
  }

  .rafa-profile-page.ipnite-profile .hero-bg {
    position: absolute;
    z-index: -1;
    inset: 0;
    min-height: 0;
    border-radius: inherit;
    background-size: 42px 42px;
    opacity: 0.55;
  }

  .rafa-profile-page.ipnite-profile .profile-header {
    grid-template-columns: 150px minmax(0, 1fr) auto;
    align-items: center;
    margin: 0;
    padding: 0;
  }

  .rafa-profile-page.ipnite-profile .photo-wrap {
    width: 150px;
    height: 150px;
    border-radius: 32px;
  }

  .rafa-profile-page.ipnite-profile .photo {
    border-radius: 26px;
  }

  .rafa-profile-page.ipnite-profile .identity {
    padding: 0;
  }

  .rafa-profile-page.ipnite-profile .eyebrow {
    color: #6b2a2f !important;
    font-family: "Roboto", sans-serif;
    font-size: 12px !important;
    letter-spacing: 0.12em;
  }

  .rafa-profile-page.ipnite-profile h1 {
    max-width: 760px;
    color: #4c0815;
    font-size: clamp(36px, 4vw, 54px);
    font-weight: 800;
    line-height: 1.04;
    text-wrap: balance;
  }

  .rafa-profile-page.ipnite-profile .headline {
    max-width: 780px;
    color: #505050 !important;
    font-size: clamp(22px, 2.3vw, 32px) !important;
    font-weight: 700;
    line-height: 1.2 !important;
  }

  .rafa-profile-page.ipnite-profile .headline em {
    color: #6b2a2f;
    font-style: normal;
  }

  .rafa-profile-page.ipnite-profile .bio-line {
    color: #5f5f64;
    font-size: 16px;
  }

  .rafa-profile-page.ipnite-profile .profile-actions {
    min-width: 168px;
  }

  .rafa-profile-page.ipnite-profile .btn {
    border-color: #6b2a2f;
    color: #4c0815;
    font-family: "Roboto Slab", serif;
    letter-spacing: 0;
    text-transform: none;
  }

  .rafa-profile-page.ipnite-profile .profile-actions .btn.primary {
    border-color: #6b2a2f;
    background: #6b2a2f;
    color: #fff !important;
  }

  .rafa-profile-page.ipnite-profile .profile-actions .btn.secondary {
    background: #fff;
    color: #4c0815 !important;
  }

  .rafa-profile-page.ipnite-profile .stats-bar {
    position: relative;
    z-index: 2;
    margin-top: 32px;
    border-radius: 24px;
    background: rgba(107, 42, 47, 0.11);
  }

  .rafa-profile-page.ipnite-profile .stat {
    background: rgba(255, 255, 255, 0.9);
  }

  .rafa-profile-page.ipnite-profile .panel,
  .rafa-profile-page.ipnite-profile .feed-card {
    border-radius: 30px;
  }

  .rafa-profile-page.ipnite-profile .panel h2 {
    color: #6b2a2f;
    font-size: clamp(27px, 2.4vw, 36px);
    font-weight: 800;
  }

  .rafa-profile-page.ipnite-profile .post-title {
    color: #505050;
    font-size: clamp(30px, 3.4vw, 46px);
    font-weight: 700;
  }

  .rafa-profile-page.ipnite-profile .post-title em {
    color: #6b2a2f;
    font-style: normal;
  }

  .rafa-profile-page.ipnite-profile .publication-card h3,
  .rafa-profile-page.ipnite-profile .project-card h3 {
    font-style: normal;
  }

  .rafa-profile-page.ipnite-profile .small-avatar,
  .rafa-profile-page.ipnite-profile .project-card > span {
    font-family: "Roboto Slab", serif;
  }

  .rafa-profile-page.ipnite-profile .contact-band {
    background: #6b2a2f;
  }

  .rafa-profile-page.ipnite-profile .contact-band h2 {
    color: #fff;
    font-weight: 800;
  }

  .rafa-profile-page.ipnite-profile .contact-band .btn {
    border-color: #fff;
    background: #fff;
    color: #6b2a2f !important;
  }

  @media (max-width: 980px) {
    .rafa-profile-page.ipnite-profile .profile-header {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .rafa-profile-page.ipnite-profile .profile-actions {
      flex-direction: row;
    }
  }

  @media (max-width: 620px) {
    .rafa-profile-page.ipnite-profile {
      padding: 14px 14px 68px;
    }

    .rafa-profile-page.ipnite-profile .profile-hero {
      padding: 34px 20px;
    }

    .rafa-profile-page.ipnite-profile .profile-header {
      gap: 22px;
    }

    .rafa-profile-page.ipnite-profile h1 {
      font-size: clamp(30px, 8.5vw, 36px);
    }

    .rafa-profile-page.ipnite-profile .headline {
      font-size: clamp(20px, 6.3vw, 27px) !important;
    }

    .rafa-profile-page.ipnite-profile .profile-actions {
      flex-direction: column;
    }

    .rafa-profile-page.ipnite-profile .stats-bar {
      margin-top: 26px;
    }

    .rafa-profile-page.ipnite-profile .orbit {
      right: -54px;
      top: -24px;
      opacity: 0.55;
    }

    .rafa-profile-page.ipnite-profile .doc-a {
      left: 12px;
      top: 24px;
      opacity: 0.5;
    }

    .rafa-profile-page.ipnite-profile .doc-b,
    .rafa-profile-page.ipnite-profile .doc-c {
      display: none;
    }
  }
</style>
